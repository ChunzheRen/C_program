#include <stdio.h> 
//strcat()将两个字符串进行合并，输出的是第一个字符串，将两个字符串强制性的合并到第一个字符串，
//但是由于字符串自己的数组特性，并不会考虑到底大小合不合适
//strncat() 有一个第三个参数，指定了最大添加的个数
//strcmp()将用户的响应和已经存储的字符串做了一下对比

//函数的变量名可以看做是一个标识符，来指定一个存储位置。
//标识符（变量名）不仅仅指定了你变量的存储类型还指定了存储的位置？ 

//标识符是名称，指定了一个储存地址的对象。
//指定了对象的表达式叫做左值。

//表达式可以是左值，左值进一步来说时可不可以修改。
//然后就是说，指针是不可以修改的，所以时一个左值。 
